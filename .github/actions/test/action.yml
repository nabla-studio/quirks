name: Test

description: Test

runs:
  using: composite

  steps:
    - name: Lint
      shell: bash
      run: pnpm exec nx affected:lint
    - name: Build
      shell: bash
      run: pnpm exec nx affected:build
    - name: Test
      shell: bash
      run: pnpm exec nx affected:test