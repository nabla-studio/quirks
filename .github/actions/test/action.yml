name: Test

description: Test

runs:
  using: composite

  steps:
    - name: Lint
      shell: bash
      run: pnpm exec nx affected:lint --exclude nextjs
    - name: Build
      shell: bash
      run: pnpm exec nx affected:build --exclude nextjs
    - name: Test
      shell: bash
      run: pnpm exec nx affected:test --exclude nextjs